const config = {
    storageAccountConnectionString: process.env.STORAGE_ACCOUNT_CONNECTION_STRING,
    domain: process.env.DOMAIN,
    openaiServerUrl: process.env.OPENAPI_SERVER_URL,
  };
  
  export default config;
  