{
  "name": "",
  "version": "1.0.0",
  "description": "",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\"",
    "inspector": "npx @modelcontextprotocol/inspector",
    "start:azurite": "azurite --silent --location ./_storage_emulator --debug ./_storage_emulator/debug.log --blobPort 10010 --queuePort 10011 --tablePort 10012",
    "init-data": "npm run build && node dist/src/scripts/InitData.js"
  },
  "engines": {
    "node": "22.17.0"
  },
  "dependencies": {
    "@azure/data-tables": "^13.2.2",
    "@azure/functions": "^4.0.0",
    "dotenv": "^16.4.5",
    "swagger-ui-dist": "^5.30.3"
  },
  "devDependencies": {
    "@types/node": "18.x",
    "@types/swagger-ui-dist": "^3.30.6",
    "azure-functions-core-tools": "^4.x",
    "rimraf": "^5.0.0",
    "typescript": "^4.0.0"
  }
}
